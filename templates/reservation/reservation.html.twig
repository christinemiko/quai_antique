 {% extends "base.html.twig" %}

 {% block body %}
     <!-- RESERVATION PAGE START -->
     <main class="container-fluid" style="padding-left: 0px;padding-right: 0px;">

         <div class=" block text-center">
             <img src="../../public/img/Logo%20ok.jpg" class="img-fluid" alt="Logo Quai Antique">
             <h1 class="mt-3 text-center text-light">Réservation</h1>
         </div>
         {{ form_start(reservationForm) }}
         {{ form_row(reservationForm.date_reservation) }}
         {{ form_row(reservationForm.hour_reservation) }}
         {{ form_row(reservationForm.number_person) }}
         {{ form_row(reservationForm.message) }}

         {{ form_end(reservationForm) }}




     </main>
     <!-- RESERVATION PAGE END -->
 {% endblock %}